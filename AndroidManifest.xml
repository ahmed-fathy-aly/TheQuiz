<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asuspt.thequiz"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <activity
            android:name="asuspt.thequiz.activities.HomeActivity"
            android:label="Home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- add   <category android:name="android.intent.category.LAUNCHER" /> here to make it the main activity-->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="asuspt.thequiz.activities.QuizActivity"
            android:label="Quiz" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- add   <category android:name="android.intent.category.LAUNCHER" /> here to make it the main activity-->
            </intent-filter>
        </activity>
        
                <activity
            android:name="asuspt.thequiz.activities.QuizGraderActivity"
            android:label="Grade" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- add   <category android:name="android.intent.category.LAUNCHER" /> here to make it the main activity-->
                </intent-filter>
        </activity>
        </application>
</manifest>
